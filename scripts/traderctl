#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${repo_root}/backend${PYTHONPATH:+:${PYTHONPATH}}"
exec python -m app.ops.cli "$@"
