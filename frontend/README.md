# Frontend

The frontend will later be a Next.js dashboard.

It will support mode display, session arming, WebSocket health, P&L status, and kill switch controls.

It must not contain trading logic, broker secrets, or credentials.
